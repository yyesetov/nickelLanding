{
  "name": "nickel-labs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 3000 --directory dist",
    "build": "node --check dist/app.js && node --check dist/demo-engine.js && node --check dist/demo.js && node --check dist/logo3d.js",
    "test": "node --test tests/*.test.mjs"
  }
}
